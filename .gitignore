# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 构建产物（自定义编译脚本产生）
build.err
build.log
compile.done
compile.log

# 发布资源包压缩包（已在仓库外单独维护，不纳入源码仓库）
XinBlog0.1.0网页端部署资源包.zip

# 运行时数据库（db/ 下为表结构 .sql，运行时生成的库文件应忽略）
*.db
*.sqlite
*.sqlite3

# 环境变量（保留 .env.example 作为模板）
.env
.env.*
!.env.example

# 依赖缓存 / 测试覆盖率
.cache
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
